/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow cross-origin requests from borrow.piotrek.app during development
  allowedDevOrigins: ['borrow.piotrek.app'],
};

export default nextConfig;
